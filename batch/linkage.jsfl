fl.outputPanel.clear();var currentDoc = fl.getDocumentDOM();var lib = currentDoc.library;                var lenLib = lib.items.length;var item;for (i=0; i<lenLib; i++) {        item = lib.items[i];        if (item.itemType == "bitmap") {                fl.trace(item.name);                                item.linkageExportForAS = true;                item.linkageExportInFirstFrame = true;                item.linkageBaseClass = "flash.display.BitmapData";                                //导出的类名                var className = item.name.substr(0, item.name.lastIndexOf("."));				                className = className.replace("/", ".");				arr = className.split("_");								className = Math.ceil(arr[0]) + "_" + Math.ceil(arr[1]) + "_" + Math.ceil(arr[2]);                item.linkageClassName = "c_" + className;                                //item.linkageImportForRS = false;                //item.linkageIdentifier = item.name;        }}fl.trace("-------------------导出项数："+lenLib);